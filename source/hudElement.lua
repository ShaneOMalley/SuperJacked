-- the 'hudElement' module
local hudElement = {}

-- the image used when rendering the hudElement
hudElement.img = nil

-- the player associated with the hudElement
hudElement.player = nil

-- call this method once per frame
function hudElement:update ()

end

return hudElement